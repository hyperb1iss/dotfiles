[storage]
# Native btrfs driver — matches the existing storage database. On Podman 6+
# this main file is overridden by .conf.d drop-ins; the authoritative driver
# selection lives in storage.conf.d/99-driver.conf. Keep both in sync.
driver = "btrfs"
