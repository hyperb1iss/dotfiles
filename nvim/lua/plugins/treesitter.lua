-- Treesitter — AstroNvim v6 runs nvim-treesitter's main branch with
-- automatic parser installation, so the archived-plugin workaround is gone.
-- Parser and textobject configuration lives in AstroCore's `treesitter` table.

---@type LazySpec
return {}
